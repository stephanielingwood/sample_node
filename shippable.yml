language: node_js

node_js:
  - "0.12"

env:
  global: 
    - FOO="foo"
    - BAR="bar"

build:
  ci:
    # - npm install
    # - grunt test
    - echo $FOO
    - echo $BAR
  on_success:
    - node myDeploymentScript.js
