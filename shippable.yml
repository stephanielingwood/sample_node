language: node_js

node_js:
  - "0.12"

build:
  ci:
    - which python
    - python --version
    - node --version
    - npm install
    - npm update
    - echo "do tests now"
  on_success:
    - echo "yay! success"

  on_failure:
    - echo "boo. failure."
