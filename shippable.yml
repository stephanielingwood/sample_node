# Language setting
language: node_js

# Version number
node_js:
  - 6.2.0

build:
  ci: 
    - source ~/.nvm/nvm.sh
    - node --version
    - npm --version
    - echo "//registry.npmjs.org/:_authToken=ABC123" > .npmrc
    - chmod 600 .npmrc
    - shippable_retry npm install  
